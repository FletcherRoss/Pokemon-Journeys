# Add this file to .gitignore — populate via Streamlit Cloud Secrets
# Settings > Secrets in your app dashboard
#
# GITHUB_TOKEN = "ghp_your_personal_access_token_here"
#
# The token needs: repo (read + write) scope on FletcherRoss/Pokemon-Journeys
# Create one at: https://github.com/settings/tokens
